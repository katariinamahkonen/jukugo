// Per-device configuration. On a PUBLIC deploy (e.g. GitHub Pages) leave the key
// BLANK and enter it in the app's Settings instead (it is stored only on your
// device and sent directly to OpenAI). Never commit a real key to a public repo.
window.__CONFIG__ = {
  openaiKey: "",
  openaiModel: "gpt-4o-mini"
};
